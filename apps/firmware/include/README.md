Headers that belong to the app entrypoint can live here.

For now, most shared interfaces are kept inside local PlatformIO libraries under `lib/SepticCore` and `lib/SepticHal` so they can be tested and reused cleanly.
